import { styled } from '../stitches.config'

export const Main = styled('main', {
  display: 'flex',
  flexFlow: 'row wrap',
  alignItems: 'stretch',
  justifyContent: 'flex-start',
  gap: '2rem',
  paddingTop: '$2',
  paddingBottom: '$2'
})
